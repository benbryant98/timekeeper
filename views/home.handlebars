{{#if session.logged_in}}
{{> daily tasks=tasks }}
{{> weekly tasks=tasks}}
{{> calendar tasks=tasks}}
{{else}}

<h1>Welcome, please login or create an account for full access</h1>

<h2>{{current_month}}</h2>

{{/if}}