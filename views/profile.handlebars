<div class="row">
  <div class="col-auto">
    <h2>Welcome, {{name}}</h2>
  </div>
</div>

<div class="row mt-4">
  <div class="col-md-6">
    <h3>Create a New Task</h3>

    <form class="form new-task-form">
      <div class="form-group">
        <label for="task-name">Task Name:</label>
        <input class="form-input" type="text" id="task-name" name="task-name" />
      </div>
      <div class="form-group">
        <label for="daily-task">Daily Tasks</label>
        <textarea
          class="form-input"
          type="text"
          id="daily-task"
          name="daily-task"
        ></textarea>
      </div>
      <div class="form-group">
        <button type="submit" class="btn btn-primary">Create</button>
      </div>
    </form>

    <form class="form new-task-form">
      <div class="form-group">
        <label for="task-name">Task Name:</label>
        <input class="form-input" type="text" id="task-name" name="task-name" />
      </div>
      <div class="form-group">
        <label for="weekly-task">Weekly Tasks</label>
        <textarea
          class="form-input"
          type="text"
          id="weekly-task"
          name="weekly-task"
        ></textarea>
      </div>
      <div class="form-group">
        <button type="submit" class="btn btn-primary">Create</button>
      </div>
    </form>

    <form class="form new-task-form">
      <div class="form-group">
        <label for="task-name">Task Name:</label>
        <input class="form-input" type="text" id="task-name" name="task-name" />
      </div>
      <div class="form-group">
        <label for="monthly-task">Monthly Tasks</label>
        <textarea
          class="form-input"
          type="text"
          id="monthly-task"
          name="monthly-task"
        ></textarea>
      </div>
      <div class="form-group">
        <button type="submit" class="btn btn-primary">Create</button>
      </div>
    </form>
  </div>

    {{#if tasks.length}}
    <div class="col-md-6 task-list">
        <h3>Current Tasks:</h3>

        {{#each tasks as |task|}}
        <div class="row mb-2">
            <div class="col-md-8">
                <h4><a href="/task{{task.id}}">{{task.name}}</a></h4>
            </div>
            <div class="col-md-4">
                <button class="btn btn-sm btn-danger" data-id="{{task.id}}">DELETE</button>
            </div>
        </div>
        {{/each}}
    </div>
    {{/if}}
</div>

<script src="./js/profile.js"></script>